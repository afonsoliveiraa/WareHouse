<%= turbo_stream_from "pas" %>

<main>
  
  <div class="row">
    <div class="col-md-6 mx-auto shadow-sm">

      <div class="header p-3">
        <%= link_to "New Projeto Atividade", new_pa_path, data: { turbo_frame: dom_id(Pa.new) } %>
      </div>
      
      <%= turbo_frame_tag Pa.new %>
      <%= turbo_frame_tag "pas" do %>
        <%= render @pas %>
      <% end %>

    </div>
  </div>
  
</main>

